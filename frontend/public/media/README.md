# 지도용 드론 영상 예시

- **`demo-drone-map.mp4`** — 홈 지도 적 마커 오버레이 및 시드 DB(`적군-ALPHA`의 `droneVideoUrl`)에서 사용하는 샘플입니다.
- Vite는 `public/`을 사이트 루트로 제공하므로, DB에는 **`/media/demo-drone-map.mp4`** 같은 경로만 저장하면 됩니다.
- 파일을 바꿀 때는 같은 이름으로 교체하거나, DB·시드의 URL 문자열만 새 경로로 맞추면 됩니다.
